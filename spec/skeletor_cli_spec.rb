require 'spec_helper'

describe SkeletorCli do
  it 'has a version number' do
    expect(SkeletorCli::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
